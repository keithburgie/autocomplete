// Note: I would normally use environment variables for this, and/or gitignore it, but I'm including the variables this way to simplify running the app after submission.
module.exports = {
  APP_HOST: "localhost",
  APP_PORT: 3030,
  SERVER_PROTOCOL: "http",
  SERVER_HOST: "localhost",
  SERVER_PORT: 3035,
};
